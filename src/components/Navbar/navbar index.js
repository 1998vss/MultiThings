import "./navbar index.css"

export default function Navbar(){
    return(
        <div id="navbarDiv">
            <button id="homeButton"><span id="homeButtonText">MultiThings</span></button>
                <input id="navSearch"type="search"/>   
        </div>
    )
}